telethon>=1.36,<2
pydantic-settings>=2.2,<3
apscheduler>=3.10,<4
fastapi>=0.111,<1
uvicorn[standard]>=0.30,<1
jinja2>=3.1.6
itsdangerous>=2.1
python-multipart>=0.0.9
qrcode>=7.4,<9
# AG-UI adapter used by the disposable Studio feasibility spike. Keep this
# exact until the dependency is reviewed against the next security release.
pydantic-ai-slim[ag-ui,openai]==2.37.0
SQLAlchemy>=2.0,<3
asyncpg>=0.29,<1
# Keep above the 2026 advisory fixes (pip-audit gate in M6/M7).
cryptography>=50.0.1,<51
# Explicit runtime dependency for bounded provider/source HTTP calls.
httpx>=0.27,<1
